.tmp/

# Slice 4 (Spec §0.5): Mobile-Toolchain und generierte Android-Projekte
# bleiben AUSSERHALB des Repos — ein `git add -A` zoege sonst ~2,1 GB ein.
# Committet in mobile/ werden nur: capacitor.config.json, package.json,
# package-lock.json, README.md (Spec §0.5).
toolchain/
mobile/android/
mobile/node_modules/
__pycache__/
